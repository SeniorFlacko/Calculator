import React from 'react';
import {Text, View} from 'react-native';
import styles from '../theme/apppTheme';

export const CalculatorScreen = () => {
  return (
    <View>
      <Text style={styles.texto}>Calculator works!</Text>
    </View>
  );
};

export default CalculatorScreen;
